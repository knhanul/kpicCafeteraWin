<Window x:Class="KpicCafeteria.Desktop.Views.BulkUpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="일괄 변경" Width="360" SizeToContent="Height" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        FontFamily="{StaticResource AppFontFamily}" FontSize="13">
    <StackPanel Margin="16">
        <TextBlock Text="변경할 항목을 선택하세요. 입력하지 않은 항목은 변경되지 않습니다." TextWrapping="Wrap"
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,12"/>

        <CheckBox Style="{StaticResource StandardCheckBox}" Content="발주일 변경" IsChecked="{Binding ChangeOrderDate}" Margin="0,0,0,4"/>
        <DatePicker Style="{StaticResource StandardDatePicker}" SelectedDate="{Binding OrderDate}" Margin="24,0,0,10" IsEnabled="{Binding ChangeOrderDate}"/>

        <CheckBox Style="{StaticResource StandardCheckBox}" Content="배송일 변경" IsChecked="{Binding ChangeDeliveryDate}" Margin="0,0,0,4"/>
        <DatePicker Style="{StaticResource StandardDatePicker}" SelectedDate="{Binding DeliveryDate}" Margin="24,0,0,10" IsEnabled="{Binding ChangeDeliveryDate}"/>

        <TextBlock Text="상태" Style="{StaticResource CaptionText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2"/>
        <ComboBox Style="{StaticResource StandardComboBox}" ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}"
                  DisplayMemberPath="Label" Margin="0,0,0,12"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource SecondaryButton}" Content="취소" IsCancel="True" Margin="0,0,8,0"/>
            <Button Style="{StaticResource PrimaryButton}" Content="적용" Click="OnConfirm" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
