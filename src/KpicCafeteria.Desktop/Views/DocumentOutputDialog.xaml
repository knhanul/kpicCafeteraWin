<Window x:Class="KpicCafeteria.Desktop.Views.DocumentOutputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="문서 출력" Width="360" SizeToContent="Height" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        FontFamily="{StaticResource AppFontFamily}" FontSize="13">
    <StackPanel Margin="16">
        <TextBlock Text="{Binding DocumentType}" FontSize="15" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>

        <TextBlock Text="시작일" Style="{StaticResource CaptionText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2"/>
        <DatePicker Style="{StaticResource StandardDatePicker}" SelectedDate="{Binding StartDate}" Margin="0,0,0,8"/>

        <TextBlock Text="종료일" Style="{StaticResource CaptionText}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,2"/>
        <DatePicker Style="{StaticResource StandardDatePicker}" SelectedDate="{Binding EndDate}" Margin="0,0,0,12"/>

        <CheckBox Style="{StaticResource StandardCheckBox}" Content="PDF로도 생성 (한컴오피스 필요)" IsChecked="{Binding GeneratePdf}" Margin="0,0,0,12"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource SecondaryButton}" Content="취소" IsCancel="True" Margin="0,0,8,0"/>
            <Button Style="{StaticResource PrimaryButton}" Content="출력" Click="OnConfirm" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
